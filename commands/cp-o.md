---
description: code-path walk — step out
allowed-tools: Read, Bash
---

# /cp-o

Continue the **active code-path walk**: treat as **`o`** (step out via `stepOut`).

Show that hop, print the control line, stop.

---
description: code-path walk — step over (skip into)
allowed-tools: Read, Bash
---

# /cp-s

Continue the **active code-path walk**: treat as **`s`** (step over — same as next spine, do not into).

Show that hop, print the control line, stop.

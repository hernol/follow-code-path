---
description: code-path walk — previous hop
allowed-tools: Read, Bash
---

# /cp-b

Continue the **active code-path walk**: treat as **`b`** (back in visit history).

Show that single hop, print the control line, stop. If no walk, suggest `/code-path`.

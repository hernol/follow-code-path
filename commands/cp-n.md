---
description: code-path walk — next spine hop (step over)
allowed-tools: Read, Bash
---

# /cp-n

Continue the **active code-path walk** in this conversation: treat as user command **`n`**.

- Advance to `stepOver` (spine) of the current hop.
- Show that single hop only, then print `> n next · b back · i into · o out · s over · q quit` and stop.
- If no walk is active, say so and suggest `/code-path <feature>`.

---
description: code-path walk — step into detail
allowed-tools: Read, Bash
---

# /cp-i

Continue the **active code-path walk**: treat as **`i`** (step into `stepInto` if present).

Show that hop, print the control line, stop. If no `stepInto`, say so and stay put.
